# Guardian — environment template.
# Copy to .env and fill in. .env is gitignored; this file is not.
#
# The API validates EVERY key in this file at boot. A missing or malformed
# value stops startup and names every offending key.
#
# The chain values depend on the sc/ contract deploy, which may not have
# happened yet — it's fine to fill those with FORMAT-VALID PLACEHOLDERS so the
# service can start. Convention: fake hex starts with 0xDEAD, zero-padded,
# ending in a repeated 4-digit role tag — 1111 escrow, 2222 funder,
# 3333 operator, 4444 guardian, 5555 deployer. Addresses are 40 hex chars
# after 0x; private keys are 64. Anthropic key placeholder starts with
# sk-ant-placeholder. Mark every placeholder line `# TODO(placeholder)` so
# `grep -n 'TODO(placeholder)' .env` lists everything still fake. The API
# detects these patterns at boot and logs a WARN naming (never valuing) the
# keys still holding placeholders — it does not block startup.
# Nothing on-chain and no LLM call will work until they are replaced.

# ---------------------------------------------------------------- Rain (STUBBED)
# Rain cannot deliver to Monad (not a supported payment-route rail, confirmed with
# Rain). Onramp/offramp endpoints exist and LOG the request they would send, but no
# call is made. Flip to true only if a Monad rail appears.
RAIN_ENABLED=false
# One team + one user for the whole platform — Rain identities are NOT per end-user.
RAIN_BASE_URL=https://api-dev.raincards.xyz/v1
RAIN_API_KEY=
RAIN_TEAM_ID=
RAIN_USER_ID=
RAIN_COLLATERAL_CONTRACT_ID=

# ---------------------------------------------------------------- Monad Testnet
MONAD_RPC_URL=https://testnet-rpc.monad.xyz
MONAD_CHAIN_ID=10143
MONAD_EXPLORER_URL=https://testnet.monadvision.com
# Canonical test USDC on Monad Testnet, 6 decimals — confirmed with Monad
USDC_ADDRESS=0x534b2f3A21130d7a60830c2Df862319e593943A3
ESCROW_CONTRACT_ADDRESS=

# All four *_PRIVATE_KEY values below are stored WITH the 0x prefix. `cast` accepts a
# bare key, so one can pass the approve step and still fail the deploy — forge's
# vm.envUint rejects bare hex outright: `missing hex prefix ("0x")`.

# Funder wallet — the source of all test money. Mint MON + test USDC here from the
# faucets; it funds user top-ups. Replaces the Rain onramp.
FUNDER_PRIVATE_KEY=
FUNDER_ADDRESS=

# Deployer — a SEPARATE throwaway key, used once by forge script. Needs MON only.
# Nothing in the running system uses it; safe to discard after deployment.
DEPLOYER_PRIVATE_KEY=

# Two separate keys on purpose (smart-contract §3.5): a leaked GUARDIAN key can only
# mis-rule a dispute, never open deals or move funds elsewhere. Both need MON for gas.
OPERATOR_ADDRESS=
OPERATOR_PRIVATE_KEY=
GUARDIAN_ADDRESS=
GUARDIAN_PRIVATE_KEY=

# ---------------------------------------------------------------- Anthropic
ANTHROPIC_API_KEY=

# ---------------------------------------------------------------- Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/guardian

# ---------------------------------------------------------------- Demo tuning
# Seconds. Production default is 86400 (24h); the demo turns it down so an
# uncontested trade visibly auto-releases on stage. Never 0 — see
# smart-contract §11.3.
REVIEW_WINDOW_SECONDS=30
SWEEPER_INTERVAL_MS=3000
