# Secrets — never commit
.env
.env.*
!.env.example
keys.txt
*.pem
*.key

# Dependencies
node_modules/

# Build output
dist/
build/
.vite/
coverage/
*.tsbuildinfo

# Solidity
artifacts/
cache/
typechain-types/

# Editors / OS
.DS_Store
*.log

# Generated by the 011 verification scripts — a captured live seed response.
# Regenerate with `node api/scripts/verify-011-seed.mjs`; the other 011 scripts
# read it so every act posts back the input that was actually published.
api/scripts/.011-seed.json
